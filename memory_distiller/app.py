"""Memory Distiller - MVP for extracting reusable LLM memory from chat logs."""

import streamlit as st

st.title("Memory Distiller")
st.markdown(
    "MVP for extracting reusable LLM memory from chat logs. "
    "Only the project skeleton exists currently."
)
